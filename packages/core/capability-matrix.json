{
  "schema": 1,
  "generatedBy": "packages/core/scripts/capability-matrix.ts",
  "rows": [
    {
      "mode": "code",
      "runtime": "winter-agent",
      "surface": "cli",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface"
    },
    {
      "mode": "code",
      "runtime": "claude-agent",
      "surface": "cli",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    },
    {
      "mode": "code",
      "runtime": "winter-agent",
      "surface": "mac",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface"
    },
    {
      "mode": "code",
      "runtime": "claude-agent",
      "surface": "mac",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    },
    {
      "mode": "code",
      "runtime": "winter-agent",
      "surface": "ios-remote",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface"
    },
    {
      "mode": "code",
      "runtime": "claude-agent",
      "surface": "ios-remote",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    },
    {
      "mode": "dispatch",
      "runtime": "winter-agent",
      "surface": "cli",
      "cell": "correctly-unavailable",
      "reason": "the TUI/CLI is code-only (packages/cli/src/session-mode.ts) — dispatch sessions are apps/orb-only and cannot be created or attached from here"
    },
    {
      "mode": "dispatch",
      "runtime": "claude-agent",
      "surface": "cli",
      "cell": "correctly-unavailable",
      "reason": "the TUI/CLI is code-only (packages/cli/src/session-mode.ts) — dispatch sessions are apps/orb-only and cannot be created or attached from here"
    },
    {
      "mode": "dispatch",
      "runtime": "winter-agent",
      "surface": "mac",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface"
    },
    {
      "mode": "dispatch",
      "runtime": "claude-agent",
      "surface": "mac",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    },
    {
      "mode": "dispatch",
      "runtime": "winter-agent",
      "surface": "ios-remote",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface"
    },
    {
      "mode": "dispatch",
      "runtime": "claude-agent",
      "surface": "ios-remote",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    },
    {
      "mode": "chat",
      "runtime": "winter-agent",
      "surface": "cli",
      "cell": "correctly-unavailable",
      "reason": "the TUI/CLI is code-only (packages/cli/src/session-mode.ts) — chat sessions are apps-only and cannot be created or attached from here"
    },
    {
      "mode": "chat",
      "runtime": "claude-agent",
      "surface": "cli",
      "cell": "correctly-unavailable",
      "reason": "the TUI/CLI is code-only (packages/cli/src/session-mode.ts) — chat sessions are apps-only and cannot be created or attached from here"
    },
    {
      "mode": "chat",
      "runtime": "winter-agent",
      "surface": "mac",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface; chat's own tool registry excludes 43 Winter built-in(s) by design (runtime-sdk/mode-options.ts's disallowedToolsFor) — narrower by mode policy, not a runtime/surface gate"
    },
    {
      "mode": "chat",
      "runtime": "claude-agent",
      "surface": "mac",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    },
    {
      "mode": "chat",
      "runtime": "winter-agent",
      "surface": "ios-remote",
      "cell": "implemented",
      "reason": "the Winter leg serves every mode (Task 17) — reachable from this surface; chat's own tool registry excludes 43 Winter built-in(s) by design (runtime-sdk/mode-options.ts's disallowedToolsFor) — narrower by mode policy, not a runtime/surface gate"
    },
    {
      "mode": "chat",
      "runtime": "claude-agent",
      "surface": "ios-remote",
      "cell": "correctly-unavailable",
      "reason": "the official claude runtime was retired (WS-23): no session is created on it, and a session recorded on it is adopted onto the Winter leg at its next resume (session-driver.ts's adoptLegacyRecord)"
    }
  ]
}
